# 実装メモ

設計書 `docx/VocaVault_PWA_v1.0.md` を正として実装した際の、差異・判断・未確認事項の記録。新しい変更ほど上に書く。

## 4 回目の変更（「＋」の動作と取込の入口、サマリーカードのボタン化、スワイプ削除の確定位置）

依頼文は「以下の 10 件」とあったが本文に書かれていたのは 3 件（「＋」と取込の入口、サマリーカード、スワイプ削除の確定位置）だけだったので、その 3 件を実装した。残りの項目は届いていないので、別途送ってもらう必要がある。

設計書の 7-2（サマリーカード）、7-3（追加（＋）、左スワイプで削除）、7-4（取込への導線の行を追加）、7-9（SummaryCard）を実装に合わせて書き換えた。示されていない節は変えていない。

### 設計書との差異・残した不整合

- **10-3 の冒頭**「単語一覧の「＋」→「CSV/TSV を取り込む」でファイルを選ぶと、単語一覧の代わりに取込画面を表示する」と、同じ表の**キャンセル**「何も書き込まず単語一覧に戻る」は古くなった。実装では入口が単語フォーム（新規）のリンクになり、取込画面はフォームの代わりに表示され、キャンセルはフォームに戻る。示された節ではないので本文は変えず、7-4 に新しい入口とキャンセルの戻り先を書いた。**7-1** の単語一覧の行にある「取込」も同様（取込画面は単語フォームのルート `#/folders/:folderId/words/new` のまま表示される）。
- **10-3 の「単語一覧に戻って結果を表示する」**はそのまま満たす（下記）。
- 2 回目・3 回目の記録に残る「40%」「＋ → `<dialog>` のアクションシート」「ActionSheet コンポーネント」は当時の記録で、現在は 20% と直接遷移に変わっている。

### 自分で判断した点

**「＋」と取込の入口（`src/ui/screens/WordList.tsx`、`src/ui/screens/WordForm.tsx`）**

- 単語一覧の「＋」は `navigate('/folders/:folderId/words/new')` を直接呼ぶ（`aria-label="単語を追加"`）。アクションシートと隠しファイル入力、取込の処理、settings の読み込みは単語一覧から削除した。「…」メニューは 2 回目の変更で既に無い。
- 取込の入口はリンクではなく `type="button"` のボタン（`.link-muted`: `--muted` 色、14px、下線、高さ 44px）にした。押すと隠し `<input type="file">` を開き、ファイルを選ぶと単語フォームの代わりに `ImportScreen` を描画する（単語一覧でやっていたのと同じ描画の仕方をフォームに移しただけで、`ImportScreen` 自体は変えていない）。URL はフォームのまま。`isEdit` のときはボタンもファイル入力も描画しない。
- ファイルを選んだ時点で `loadSettings()` も読み、取込画面の初期値（前回の区切り・見出し・割り当て）に使う。単語一覧のマウント時に settings を読む必要が無くなったため。
- **取込画面のキャンセル**は単語フォームに戻る（`importFile` を null にするだけ）。入力途中の英単語などは残る。
- **取込後**は `navigate(一覧, { replace: true, state: { message } })` で単語一覧に戻り、単語一覧が `location.state.message` を初期値として通知欄に出す（表示位置と `data-testid="import-result"` は以前と同じ）。受け取った直後に `navigate(location.pathname, { replace: true, state: null })` で履歴から消し、再読み込みや戻る操作で同じ結果がもう一度出ないようにした。失敗時（保存失敗・容量超過）も同じ経路でメッセージを渡す。
- `ActionSheet` コンポーネントは使う場所が無くなったので削除した（`.sheet-actions` の CSS も）。ホームのフォルダメニューは自前の `<dialog>` なので影響なし。
- 設定画面の取込の説明文を新しい入口に合わせて書き換えた。
- 単語一覧の空状態「単語がありません。＋で追加するか取込してください」は、取込も「＋」の先にあるので変えていない。

**サマリーカード（`src/ui/components/SummaryCard.tsx`）**

- 3 状態とも `<section class="card summary-card">` に `<h2>` を置き、上 2 つはその下に `btn-primary` のボタン（`.summary-btn`: 高さ 52px、17px）。カード自体はボタンではなくなり、`.card.tappable` の CSS は削除した。`onStartReview` / `onStartNew` の遷移先（`/study/select?scope=all`）は以前のまま。
- 見出しは 17px。②の見出し「今日の復習はありません。新しい単語が N語あります」は iPhone の幅では 2 行に折り返すので、`h2` に `min-height: 2.8em`（2 行分）を持たせて①と②のカードの高さが揃うようにした（390px 幅で両方 144px）。①では見出しの下に 1 行分の余白ができる。3 行に折り返す場合（極端に大きい N や文字サイズ）は揃わない。
- ③（どちらも 0）は `.empty` でカード全体を `opacity: 0.55` にし、見出しの `min-height` は外す。
- ①の「·」は U+00B7（中点）。予想時間は既存の `estimateMinutes()`。
- `data-testid="summary-card"` はカードに残し、ボタンに `data-testid="summary-action"` を付けた。

**スワイプ削除（`src/ui/components/SwipeRow.tsx`）**

- `DELETE_RATIO` を 0.4 → 0.2 に変えただけ。ゴミ箱の不透明度と拡大率は同じ定数を使っているので、20% で全部見えるようになる（以前は 40%）。「元に戻す」の 5 秒（`UNDO_MS`）は変えていない。

### 確認できなかった点

- iPhone 実機での取込リンク → ファイル選択の挙動（Chromium では `setInputFiles` で代替）。以前の「＋」メニューからの経路と同じ隠し `<input type="file">` を使っているので、実機の挙動は変わらない想定。
- 20% の確定位置の手触り（誤って削除しやすくなっていないか）。

### ヘッドレスブラウザで確認した内容

`npm run preview` に対して Playwright（Chromium、iPhone 13 相当 390×844 のタッチ端末エミュレーション）で 53 項目を確認した: 空のサマリーカード（`.empty`、見出しのみ、ボタンなし、不透明度 < 1） → フォルダ作成 → 単語一覧に「追加メニュー」と「…」が無いこと → 「＋」で `dialog` を開かずに単語フォームへ直接遷移 → リンクの文言・色（`--muted`）・位置（メモ欄の下、保存ボタンの上）・高さ 44px → ファイル選択で取込画面（URL はフォームのまま、プレビュー 4 行） → キャンセルでフォームに戻り入力が残る → 取り込むと単語一覧に戻って「3件を取り込みました」、3 行、履歴の state が消えていること、再読み込みで結果が再表示されないこと → 編集画面にリンクとファイル入力が無いこと → スワイプ削除（マウス 15% で戻る、タッチ 18% の途中でゴミ箱が部分表示・離すと戻る、マウス 25% でトーストと行の非表示・リンクへ遷移しない、「元に戻す」、タッチ 22% で削除、5 秒後にトーストが消えて再読み込み後も 2 行） → New ありのカード（見出し文言、ボタン文言、強調色 `--accent` と白文字、高さ 52px 以上、見出しのタップでは遷移しない、ボタンで `#/study/select?scope=all`、モード選択のタイトル） → IndexedDB を直接書き換えて復習あり（見出し「今日の復習: 2語 · 約1分」、ボタン「復習を始める」、New ありと同じ背景色・高さ・幅、ボタンも同じ配色・大きさ、ボタンで「全フォルダを復習する」） → 検索中もボタンが残る → 320px 幅のホームと単語フォームで横スクロールなし → ダークモードのボタン色。ページエラーなし。

## 3 回目の変更（スワイプ評価の変更、評価の取り消し）

設計書の 6-3、6-4、7-6、12-1 を実装に合わせて書き換えた。加えて 14 章の項目 4 が「表面では反応しない」のままだと新しい 6-4 と矛盾するので、表面・裏面の両方で動くことと取り消しの確認項目（4-2）に書き換えた。示されていないほかの節は変えていない。

### 設計書との差異・残した不整合

- **`UndoEntry` に `prevDue`（評価前の `lastDue[wordId]`）を足した**。`lastDue` は初期実装で追加した結果画面用の項目（設計書 6-3 の `SessionState` には無い）で、これも評価前に戻さないと「次回最も早い復習日」が取り消した評価を含んでしまうため。設計書 6-3 の `UndoEntry` には `lastDue` 自体が無いので書いていない。
- **`shownCount` は取り消しで戻さない**。表示に使っていない項目で、戻った単語を再表示するときに `markShown()` がまた +1 する。

### 自分で判断した点

**スワイプ評価（`src/ui/screens/Flashcard.tsx`）**

- **軸の決定と追従**: `pointerdown` で `setPointerCapture` し、10px 動いた時点で `|dx| >= |dy|` なら横、そうでなければ縦に決める。以後はその軸の移動量だけを使い、カードの追従もその軸方向だけにした（横に決めた後に指が上下しても、カードは横にしか動かない）。「指に追従」を 2 軸で動かすと、判定に使わない方向にもカードが動いて評価が読みにくくなるため。
- **タップの判定**: 10px 未満のまま離したらタップ（軸が決まっていない = タップ）。以前の 8px から 10px に揃えた。`click` ハンドラでの反転は削除し、`pointerup` とキーボード（Enter / Space）だけで反転する。マウスでドラッグして戻したときに `click` が発火して裏返ってしまうのを避けるため。
- **色の重ね方**: カードの `background` そのものは変えず、カード全体を覆う `.swipe-fill`（評価色、`border-radius: inherit`）と、その上に `.swipe-grade`（評価名、白、44px）を絶対配置で重ねた。どちらも不透明度 = 0.9 × min(1, 移動量 ÷ 120)。内容（英単語や日本語訳）は覆いの下に残るので、120px で 1 割だけ透けて見える。評価名と本文が同じ中央に重なるが、覆いが濃くなるにつれ本文が消えるので実用上は読める。
- **傾き**: 横スワイプのみ `rotate(8deg × clamp(移動量 ÷ 120, -1, 1))`。縦は傾けない。
- **確定後の動き**: 120px 以上で離したら `.leave` クラス（`transition: transform 0.25s`）で画面外（横: `innerWidth + 200px`、縦: `innerHeight + 200px`）へ動かし、250ms 後に `rate()` を呼ぶ。評価を先に呼ぶと、抜けていく途中のカードの中身が次の単語に変わってしまうため。抜けている間は評価ボタン・↶・カードの `pointerdown` を無効にする。次のカードは `word` が一度 null になってからマウントされるので、抜けた位置から戻ってくる見た目にはならない。
- **120px 未満で離したとき**: `.settle` クラス（`transition: transform 0.2s`）で元の位置へ。戻っている途中でも `pointerdown` は受け付ける（その時点でアニメーションを打ち切って指に付く）。
- **`touchmove` の抑止**: カード要素に `addEventListener('touchmove', …, { passive: false })` を付け、ジェスチャー中（`pointerdown` 〜 `pointerup` / `pointercancel`）だけ `preventDefault()` する。React の `onTouchMove` は passive になるので使えない。カードは単語が切り替わるたびにマウントし直されるため、リスナーは `word.id` を依存にした `useEffect` で付け直す。
- **`overscroll-behavior: none`** は指示どおり学習画面（`.screen.study-screen`、フラッシュカードとタイプ入力の両方）に付けた。iOS のラバーバンドは最上位のスクロール要素（html / body）で起きるので、実機でまだ端が引っ張れる場合は `html, body` 側にも付けるのが次の手。
- **評価ボタンの場所**: 表面では `.grade-area` を `visibility: hidden` にして場所だけ確保する（`aria-hidden` も付ける）。表裏でカードの大きさが変わらないようにするため。ボタン自体も表面では `disabled`。
- **左上の評価表記（`.swipe-label`）とその CSS は削除**した。
- **PC のマウス**でも同じコードで動く（Playwright の確認はマウスと CDP のタッチイベントの両方で行った）。

**評価の取り消し（`src/app/session.ts`、`src/db/repo.ts`、`src/ui/useStudy.ts`）**

- **`applyRating()` の引数を変更**: `(state, wordId, grade, updated, now)` → `(state, before: Word, grade, updated: Word, logId, now)`。評価前の FSRS 項目と ReviewLog の id を `UndoEntry` に控えるため。`before` から FSRS 項目だけを `FSRS_KEYS` で抜き出して保存する（`Word` 全体は持たない）。
- **取り消しの順序**: `undoAndSave()` は先に DB（`repo.revertRating()`: `words.update(wordId, before)` と `reviewLogs.delete(logId)` を 1 トランザクション）を戻し、成功したときだけ `undoLast()` でセッション状態を戻す。失敗したらセッション状態は変えず「取り消しに失敗しました」を表示する（`rateAndSave()` と同じく 1 回再試行、容量超過は専用メッセージ）。
- **`updatedAt` は戻さない**。評価時に付いた `updatedAt` が残るが、表示にも判定にも使っていない。
- **`finishedAt` は取り消しで消す**。最後の 1 枚の評価直後は結果画面へ遷移するので学習画面から取り消せる状況は無いが、状態として矛盾しないようにした。
- **保存失敗でスキップした評価**（`skipCurrent()`）は `UndoEntry` を積まない（DB に何も書いていないため）。その後に取り消すと、スタックの末尾（スキップより前の評価）の位置まで戻るので、スキップした単語ももう一度出る。
- **フラッシュカードを裏面で戻す方法**: `useStudy` が `restoredKey`（戻った出題位置 `index:wordId`）を **ref** に持ち、`setSession()` の前に書く。`Flashcard` は出題位置（`cardKey`）が変わる `useEffect` の中で `isRestored(cardKey)` を見て `flipped` の初期値を決める。最初は React の state で持ったが、`setSession()`（`useSyncExternalStore` の外部ストア更新 = SyncLane）と `setState`（DefaultLane）が別の描画になり、出題位置が変わった描画で state がまだ古く、表面で出てしまった。ref なら同じ描画で参照できる。
- **タイプ入力**は出題位置が変わると入力欄と判定結果を消す既存の `useEffect` がそのまま働くので、追加の処理は無い。
- **↶ ボタン**は共通ヘッダー（`Header`）を使っていない学習画面のヘッダー左側に直接置いた（`aria-label="直前の評価を取り消す"`）。取り消せるものが無いとき、保存中、カードが抜けている間は非活性。確認ダイアログは出さない。
- **結果画面から取り消す機能は無い**（指示どおり学習画面だけ）。

### 確認できなかった点

- iPhone 実機でのスワイプの手触り: `touch-action: none` と `setPointerCapture`、非 passive の `touchmove` 抑止、`overscroll-behavior: none` の効き方（特にラバーバンド）。Chromium では CDP のタッチイベント（`Input.dispatchTouchEvent`）とマウスの両方で 4 方向の評価・軸ロック・戻りを確認したが、iOS Safari の挙動は別。
- 抜けるアニメーション（250ms）と次のカードの出方の実機での見え方。

### ヘッドレスブラウザで確認した内容

`npm run preview` に対して Playwright（Chromium、iPhone 13 相当 390×844 のタッチ端末エミュレーション）で 49 項目を確認した: 表面の案内文「タップで答えを表示。スワイプで評価」、開始直後の ↶ 非活性、表面での `.grade-area` の `visibility: hidden` とボタン非活性、カードの `touch-action: none`、学習画面の `overscroll-behavior: none`、静止時に評価名が無いこと → 表面で右 60px: 「Good」、覆いと評価名の不透明度 0.45、`translate(60px) rotate(4deg)`、離すと戻って評価されない（ReviewLog 0 件、表面のまま） → 表面で右 130px: 不透明度 0.9、傾き 8 度、離すと `.leave` で画面外へ、Good で保存（state = Learning、ReviewLog 1 件）、次のカードは表面、↶ が活性 → ↶: 同じ単語が裏面（評価ボタン有効）で戻る、ReviewLog 0 件、単語が New（reps 0）に戻る、進捗が「残り 3枚 / 完了 0枚」、↶ が非活性 → 裏面で下 130px: 「Again」、傾き無し、再出題で残りが減らない → 横 12px で軸を決めた後に縦 200px 動かしても横（Hard、`translate(-188px)`）のまま → 4px の移動はタップとして裏返る、裏面で `.grade-area` が visible → CDP のタッチで上 140px: 「Easy」で保存 → ↶ を繰り返して開始時点（残り 3 / 完了 0、ReviewLog 0 件、↶ 非活性、最初の単語）に戻る → ボタンで最後まで評価して結果画面（Easy 3 件） → 進捗リセット後に英→日タイプ入力: ↶ 非活性、不正解 → 次へ（Again）→ ↶ で同じ出題に戻り入力欄が空・判定表示なし・入力可能、進捗が戻る → 320px 幅で横スクロールなし。ページエラーなし。

## 2 回目の変更（評価ボタン表記、取込画面、複数選択・スワイプ削除、横断検索ほか）

設計書の 4-5、5-2、5-3、5-4、6-4、7-2、7-3、10-3、12-1 を実装に合わせて書き換えた。示されていない節は変えていない。

### 設計書との差異・残した不整合

- **付録 B-3 の `learning_steps: ['10m']`** は、変更対象に含まれていない節なのでそのままにした。5-2 の本文とコード（`['10m', '10m']`）が正で、B-3 は「骨子」として読む。
- **`docx/VocaVault_PWA_v1.0.md` の 12-1 `app/csv.ts` の行**（「カンマ / タブ判定、ヘッダー判定、引用符、重複、形式エラーの件数」）は変えていないが、テストは新しい API（セミコロン、見出し語からの割り当て、前回値の引き継ぎ、列の割り当て）も覆う。
- **Dexie のスキーマ版数とバックアップの `schemaVersion` は 1 のまま**。settings に追加した 3 項目（`importDelimiter` / `importHasHeader` / `importColumns`）はインデックスを持たず、読み込み時に `DEFAULT_SETTINGS` とマージするので、旧バックアップも旧 DB もそのまま読める。

### 自分で判断した点

- **評価ボタンの表記**: `GRADE_LABELS`（もう一度 / 難しい / 普通 / 簡単）を削除し、既存の `GRADE_NAMES`（Again / Hard / Good / Easy）に一本化した。結果画面は英語 1 行だけにした（以前は英語と日本語の 2 行）。フラッシュカードのスワイプ中のラベルも同じ表記。
- **プレビュー（↻ / N日後）の判定**: 「当日中に再出題される」は due の日付ではなく、更新後の state が 1 か 3 か（= 6-3 手順 4 でキュー末尾に追加される条件）で判定する。`isShortTermState()` を `domain/fsrs.ts` に置き、`session.applyRating()` も同じ関数を使うので、ボタンの「↻」と実際の再出題が必ず一致する。23:55 の評価で 10 分ステップが翌日に食い込む場合も「↻」になるが、セッション内では再出題されるので表示として正しい。「N日後」は `scheduled_days`（Review なら 1 以上）。
- **↻ の注記行**は、4 つのボタンのどれかが「↻」のときだけ描画し、それ以外は行ごと出さない（レイアウトは 1 行分動く）。タイプ入力の Good / Easy ボタンにはプレビューを付けていない（6-4 のタイプ入力の記述にプレビューがないため）。
- **フォルダカードと単語行**: ホームのフォルダは `📁` 絵文字付きのカード（`--surface` 背景、1px の枠、薄い影、角丸 12px）。単語一覧と検索結果は背景なし・角丸なしで、`--border` の区切り線だけのリスト行。絵文字はアイコン画像を増やさないための選択で、iOS では標準で描画される。
- **「…」メニューの撤去**: 取込しか入っていなかったので、ボタンごと削除した。取込の入口は「＋」→ `<dialog>` のアクションシート（新規 `ActionSheet` コンポーネント）。ホームのフォルダメニューは既存の実装のままにした。
- **複数選択**: 選択モード中は行の `<label>` 全体でチェックを切り替え、編集画面へは遷移しない。右上は「完了」だけになり「＋」と「選択」は隠す。削除は `deleteWords(ids)`（`deleteWord` はこれを使うよう書き換え）で ReviewLog も含めて 1 トランザクション。削除後は選択モードを抜ける。単語 0 件なら「選択」は非活性。
- **スワイプ削除**（`SwipeRow` コンポーネント）: Pointer Events で実装し、`touch-action: pan-y` で縦スクロールはブラウザに任せる。10px 動くまでは判定しない。横が縦より大きければスワイプ（`setPointerCapture`）、そうでなければ以後無視。左方向のみ（右へは動かない）。ゴミ箱は行の右端に赤背景で置き、移動量 ÷ (行幅 × 40%) を不透明度と拡大率にして徐々に出す。40% 以上で離すと `onDelete`、未満なら 0.2 秒のアニメーションで戻す。判定できる距離を動かした後の `click` は捕捉して潰し、行のリンクへ遷移しない。PC のマウスでも動くよう、リンクのネイティブドラッグ（`dragstart`）を止めている。
- **削除の保留**: スワイプ確定で行を一覧から隠し、`{ word, timer }` を ref に持つ。5 秒後、または単語一覧のアンマウント（戻る、編集画面へ、学習開始など）の時点で `deleteWord()` → バッジ更新 → 再読み込み。「元に戻す」はタイマーを止めて行を戻すだけ。保留中に別の行をスワイプしたら、先の保留分を先に確定してから新しい保留にする（トーストは常に 1 件）。アプリを閉じた（ページを破棄した）場合は削除されず単語が残る。設計書に記載のない挙動なので、安全側（残る）にした。
- **取込画面**: 別ルートにせず、単語一覧が `importFile` を持っている間だけ `ImportScreen` を描画する（ファイル内容はメモリ上にあるだけで、再読み込みで消える）。初期値の優先順（見出し語 > 前回の保存値 > 既定の並び）と、区切り・見出しの変更時に割り当てを作り直すことは 10-3 に書いた。前回の区切りは「1 行目にその文字が含まれるとき」だけ優先し、含まれなければ自動判定に落とすので、CSV の後に TSV を読んでも誤判定しない。区切りのセレクトの `value` にはタブ文字を入れず `comma / tab / semicolon` のキーを使う。
- **見出し語**: `startsWith`（小文字化・BOM 除去・trim 後）で判定する。旧実装は 1 列目が「英単語 / englishTerm / english」で始まるときだけ見出しとしていたが、新実装はどの列でもよく、「日本語訳」「メモ」だけの見出しでも ON になる。
- **列の割り当ての一意性**は UI 側で担保する（同じ役割を別の列で選ぶと元の列は「使わない」に戻る）。`hasEnAndJa()` は念のため件数も確認する。
- **settings の保存タイミング**: 「取り込む」を押した直後、単語を書き込む前に保存する。取込がバリデーション等で 0 件でも設定は残る。
- **セッション終了の遷移（8）**: Chromium（Playwright）ではボタン・スワイプ・タイプ入力のいずれの経路でも、再出題分を含めて最後の評価の直後に結果画面へ遷移し、報告された現象は再現できなかった。iPhone 固有の要因として疑わしかった「`updateBadge()`（`navigator.setAppBadge`）の完了を待ってから遷移する」構造をやめ、評価を反映した直後に `navigate('/study/result')` を呼ぶよう `useStudy` を変えた。バッジは結果画面の表示時に更新される（既存の処理）ので、遷移がバッジ更新に依存しなくなった。`✕` の途中終了も同じ関数で遷移する。実機で再発する場合は、この変更でも直っていないことになるので、その旨を知らせてほしい。
- **横断検索**: 1 文字以上入力したときだけ `listAllWords()` で全単語を読む（ホーム表示のたびには読まない）。300ms のデバウンス、英語・日本語訳の小文字部分一致、英単語順、100 件で打ち切って「さらに絞り込んでください」を出す。検索中もサマリーカード・ストリーク・状態別割合は表示したまま、フォルダ一覧だけを結果に置き換える（7-2 の記述どおり）。結果行のタップ先は単語フォームで、そこから戻るとホームではなくその単語のフォルダに戻る（単語フォームの既存の戻り先）。
- **設定画面の取込の説明文**を新しい入口（＋ → CSV/TSV を取り込む）と取込画面の内容に合わせて書き換えた（7-8 の「CSV/TSV 取込の説明」の範囲内）。

### 確認できなかった点

- iPhone 実機でのスワイプの手触り（`touch-action: pan-y` と Pointer Events の組み合わせ、縦スクロールとの競合、リンクの長押しプレビューの抑止 `-webkit-touch-callout: none`）。Chromium のタッチエミュレーションとマウスでは、横スワイプ・縦スクロール・タップ遷移の切り分けを確認した。
- iPhone 実機でのセッション終了の自動遷移（上記）。
- `<select>` の見た目（iOS のピッカー）とプレビュー表の横スクロール。

### ヘッドレスブラウザで確認した内容

`npm run preview` に対して Playwright（Chromium、390×844 のタッチ端末エミュレーション）で 68 項目を確認した: フォルダカードのアイコン・枠・角丸 → 単語一覧の「…」がないこと、「＋」メニューの 2 項目、区切り線の行 → 選択モード（チェックボックス、「0件を削除」非活性、「2件を削除」、確認文言、削除後にモード解除、「完了」で解除） → スワイプ削除（40% 未満で戻る、ドラッグ中のゴミ箱の部分表示、縦方向のドラッグは無視、40% 以上でトースト表示と行の非表示、「元に戻す」で復元、5 秒後に確定して再読み込み後も消えていること、保留中に画面遷移して確定） → 取込画面（先頭 5 行のプレビュー、見出し行の強調、区切りの自動判定、見出し語からの割り当て、英単語未選択で非活性と案内文、同じ役割を選ぶと他列が「使わない」に戻る、見出しトグル、タブに変えると 1 列になり非活性、カンマに戻すと割り当て復元、6 件の取込結果、引用符内カンマの保持、2 回目にセミコロン区切りファイルで前回の見出し設定と割り当てが初期値になること、キャンセル） → ホームの横断検索（英語・日本語訳、フォルダ名表示、フォルダ一覧の置き換え、該当なし、タップで編集画面、空にすると戻る） → フラッシュカードの Again / Hard / Good / Easy 表記、New カードの「↻ ↻ ↻ N日後」、注記行、分表示がないこと、再出題を含む 20 回の評価後に自動で結果画面 → 結果画面の内訳表記 → タイプ入力の Good / Easy 表記と再出題後の自動遷移 → 320px 幅で横スクロールなし。ページエラーなし。

## 初期実装

## 設計書との差異

- **FSRS の learning_steps を `['10m']` から `['10m', '10m']` に変更**（`src/domain/fsrs.ts`）。2 回目の変更で設計書 5-2 / 5-3 / 12-1 もこの値に合わせたので、現在は差異ではない（付録 B-3 のみ旧値のまま）。当時の理由:ts-fsrs 5.4.2 の `BasicLearningStepsStrategy` は、New カードで Good を押すと「次のステップ」に進む扱いで、ステップが 1 段だと Good で直接 Review（2 日後）に進む。設計書 5-2 の理由欄「新しい単語はセッション内で 1 回再出題されてから Review に進む」、5-3 の遷移「New → Good → Learning（10 分後）→ Good → Review」、12-1 のテスト「New に Good で state = 1 と due が約 10 分後」を満たすため、学習ステップを 2 段にした。副作用として New に Hard を押した場合は 10 分後 → もう 1 度 10 分後 → Review と 2 回再出題される。`relearning_steps` は `['10m']` のままで、Review → Again → Relearning（10 分後）→ Good → Review と設計どおり 1 回再出題になる。設計書の文字どおり `['10m']` に戻す場合はこの 1 行を変更し、`fsrs.test.ts` の「New + Good」の期待値を state = 2 に直せばよい。
- **`index.html` に `<link rel="icon" href="icons/icon-192.png">` を追加**。8-2 の head には無いが、ブラウザが自動要求する `/favicon.ico` の 404 を避けるため。外部通信は増えない。
- **Vitest は 3.x**。4.x は npm 10.9 の依存解決バグ（`Cannot read properties of null (reading 'edgesOut')`）でインストールできなかった。Vite 7.3、@vitejs/plugin-react 5.2、TypeScript 5.9、React 18.3、react-router-dom 6.30、Dexie 4.4、ts-fsrs 5.4.2、vite-plugin-pwa 1.3、fake-indexeddb 6.2。
- **アプリ情報のバージョン**は Vite の `define` で `__APP_VERSION__` に `package.json` の version を注入して表示する。
- **`SessionState` に `lastDue: Record<string, number>` を追加**。結果画面の「次回最も早い復習日」（6-6）に必要な「評価した単語の更新後 due」を持つため。`context` は `{ scope: 'all' | folderId, kind: 'review' | 'new' }` で 6-1 の 4 種を表す。
- **リポジトリ構成に無いファイルを追加**: `src/ui/hooks.ts`（セッション購読・非同期ロード）、`src/ui/useStudy.ts`（学習画面共通の評価・保存・終了処理）、`src/ui/SwContext.tsx`（更新通知を設定画面と共有）、`src/ui/components/Header.tsx`、`InputDialog.tsx`（フォルダ名入力）、`Switch.tsx`。`scripts/gen-icons.mjs`（PNG 生成。zlib のみで依存追加なし。`npm run icons`）。生成した PNG はリポジトリに含めた。
- **DB リポジトリに追加した関数**: `countReviewsBetween`（結果画面の「当日初の学習」判定用）、`readSnapshot` / `replaceAll` / `clearAll`（バックアップ用）。

## 自分で判断した点

- 単語フォームの「必須項目が空」は保存ボタンの非活性だけで示し、赤字は文字数超過のときだけ出す（7-4 は「超過項目の下に赤字で理由」）。
- フォルダ行の「…」は `<dialog>` のアクションシート（名前を変更 / 削除 / キャンセル）。
- モード選択でキューが空のときのタイトルは復習側の文言（「全フォルダを復習する」「学習を始める（フォルダ名）」）。
- 単語一覧の検索は英語・日本語訳を小文字化した部分一致。300ms のデバウンス。
- 結果画面の「当日初の学習」は、セッション開始時刻より前に当日の ReviewLog が無かったかどうかで判定する。
- フラッシュカードのスワイプは Pointer Events で実装。8px 未満の移動はタップとして反転に使う。移動量 60px 以上で方向確定。表面ではスワイプもボタンも無効。
- タイプ入力では判定後にメモを表示する（設計書に記載なし。表面では見せない）。
- 結果画面で 1 枚も評価していないときの「次回最も早い復習日」は「—」。
- 10-4 の「書き込み失敗は 1 回再試行」は評価保存（`rateAndSave`）に実装。その他の書き込みは失敗時にメッセージを表示するだけ。`QuotaExceededError` は「端末の空き容量が不足しています」。
- バックアップ読み込み時の settings は既定値とマージし、`id` と `schemaVersion` を固定する。
- 通知の `finishToday()` は設計どおり `lastNotifyScheduledAt` / `lastNotifyItemCount` を保存してから URL を開く。件数 0 の日は項目に含めないため、`lastNotifyItemCount` は「項目のある日数」になる。
- `npm run build` の `base` は `BASE_PATH` 環境変数（既定 `/`）。GitHub Actions では `/<リポジトリ名>/`。
- 8-5 の永続化要求は、最初のフォルダ作成または単語登録時に一度だけ呼ぶ。
- バッジ更新は起動時（ホーム表示時）、セッション完了時、単語の追加・削除・取込・進捗リセット・バックアップ読み込み時に行う。

## 確認できなかった点

- iPhone 実機の挙動全般（standalone 表示、共有シートからの「ファイルに保存」、`shortcuts://` URL の起動、アプリバッジ、`visualViewport` によるスクロール調整、`lang` 属性によるキーボード切替、ダークモードの実機表示）。第 14 章の項目は利用者確認。
- `navigator.share` によるファイル共有: ヘッドレス Chromium では `canShare` が無いため `<a download>` の経路のみ確認した。
- `navigator.setAppBadge`: ヘッドレス Chromium では未対応のため no-op になることのみ確認。
- Service Worker の更新バナー: 登録は確認したが、2 回目のデプロイで `needRefresh` が立つ流れは再現していない。
- GitHub Actions のデプロイ: ワークフローは設計書 13-3 のとおりに置いたが、main へのマージ後にしか実行されない。Settings → Pages の Source を「GitHub Actions」にする作業は手動。
- Shortcut「VocaVault通知」本体（9-6 のとおりエージェントは作成・検証できない）。

## ヘッドレスブラウザで確認した内容（12-2 の 3）

`npm run preview` に対して Playwright（Chromium）で、ホーム表示 → フォルダ作成（重複名の拒否） → 単語登録 3 件（文字数超過の赤字と保存非活性、変更破棄の確認） → 検索・フィルター・件数表示 → CSV 取込（BOM、ヘッダー、引用符内カンマ、重複、形式エラーの件数報告）と TSV 取込 → 単語の編集と削除 → 設定変更（最大枚数、表示順、通知 ON、時刻、日数）の永続化 → フラッシュカード 1 周（表面でボタン無効、反転、プレビューラベル、右スワイプ = Good、結果画面、ストリーク表示） → 英→日タイプ入力 1 周（不正解の ✕ と正解表示、正解の ✓、スキップ、`lang` 属性） → 日→英の途中終了 → バックアップ書き出し（ファイル名と内容） → 進捗リセット → 不正ファイルの拒否とバックアップ読み込みによる復元 → フォルダ名変更 → ダークモードの背景色 → 320px 幅で横スクロールなし → `sw.js` と `manifest.webmanifest` の配信と Service Worker 登録、を確認した（70 項目すべて成功、ページエラーなし）。
